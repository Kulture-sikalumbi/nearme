services:
  - type: web
    name: nearme-geofence
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn geofence_server:app --host 0.0.0.0 --port $PORT
    autoDeploy: true
